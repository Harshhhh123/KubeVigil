name: Drift Engine CI/CD

on:
  push:
    paths:
      - 'backend/kubevigil/drift-engine/**'
    branches: [main]

env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: kubevigil/drift-engine

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag and push image
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
          ./backend/kubevigil/drift-engine
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

    - name: Update k8s manifest
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        sed -i "s|image: .*kubevigil/drift-engine.*|image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG|g" \
          k8s/drift-engine-deployment.yaml
        git config user.email "actions@github.com"
        git config user.name "GitHub Actions"
        git add k8s/drift-engine-deployment.yaml
        git commit -m "ci: update drift-engine image to $IMAGE_TAG"
        git push