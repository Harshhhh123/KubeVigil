# KubeVigil
KubeVigil is an open-source GitOps drift detection platform that continuously compares your live Kubernetes cluster state against your Git repository and uses an AI agent to explain exactly why the drift happened, who caused it, and what broke because of it — then opens a remediation PR automatically.
